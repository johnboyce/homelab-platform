# Cockpit env example (HOST-ONLY in real deployments)
# Copy to: /etc/homelab/secrets/cockpit.env
#
# NOTE: Do not commit real values.
# Cockpit uses PAM authentication by default (system users)

# Optional: Configure Cockpit to allow root login
# COCKPIT_ALLOW_UNENCRYPTED=true

# Optional: Configure Cockpit certificate path
# COCKPIT_CERT_PATH=/etc/cockpit/ws-certs.d
