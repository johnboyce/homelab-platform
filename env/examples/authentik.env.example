# Authentik env example (HOST-ONLY in real deployments)
# Copy to: /etc/homelab/secrets/authentik.env
#
# NOTE: Do not commit real values.

# Authentik secret key - generate with: openssl rand -base64 64
AUTHENTIK_SECRET_KEY=REPLACE_ME

# PostgreSQL database credentials
AUTHENTIK_POSTGRESQL__PASSWORD=REPLACE_ME

# Redis password (must match redis.env)

# Outpost token - generate in Authentik UI under Applications > Outposts
AUTHENTIK_TOKEN=REPLACE_ME

# Error reporting (optional)
AUTHENTIK_ERROR_REPORTING__ENABLED=false

# Logging
AUTHENTIK_LOG_LEVEL=info
