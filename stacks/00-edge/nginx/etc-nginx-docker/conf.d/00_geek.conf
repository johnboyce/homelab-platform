server {
  listen 80 default_server;
  server_name geek;

  location = /healthz {
    add_header Content-Type text/plain;
    return 200 "ok\n";
  }

  location / {
    add_header Content-Type text/html;
    return 200 "<h1>GEEK Edge is up</h1><p>nginx container is serving traffic.</p>";
  }
}
